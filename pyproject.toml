[project]
name = "website-agent"
version = "0.1.0"
description = "AI agent for building professional websites"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "langgraph",
    "langchain",
    "pydantic",
    "python-dotenv",
    "langchain-openai",
    "psycopg[binary]",
    "langgraph-checkpoint-postgres",
]
